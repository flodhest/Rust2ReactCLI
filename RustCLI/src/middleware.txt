// YourMiddlewareFile.ts

// This is a placeholder file for your Middleware.
// Feel free to replace the content based on your requirements.

const exampleMiddleware = (store) => (next) => (action) => {
  // Your middleware logic goes here
  return next(action);
};

export default exampleMiddleware;
